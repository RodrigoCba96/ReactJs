const CartWidget = () => {
    return (
        <div>
          <p class= "nro">  <img class='carro'src={process.env.PUBLIC_URL + "./img/carrito.png"} />2</p>
        </div>
    );
};

export default CartWidget;